/* Supabase config — fill these in from your project (Settings -> API -> Project URL / anon
   public key), then create the table below. The anon/public key is SAFE to ship in client
   code; that's how Supabase's client-side model works — real protection comes from Row
   Level Security policies on the table, not from hiding this key. See README.md for the
   exact SQL, and for the security trade-off of syncing with a code instead of real accounts.

   Leave the placeholders as-is to run without cloud sync — "My Agency" and "Saved requests"
   just stay in this browser's localStorage, exactly like before this file existed. */
window.SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
